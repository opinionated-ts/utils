import { describe, expect, it } from "bun:test";

/**
 * Template example test.
 *
 * This is only a placeholder to demonstrate the test structure
 * and ensure the CI pipeline is working.
 *
 * Replace it with your own tests when starting a new project.
 */
describe("template example", () => {
  it("should pass as a demonstration test", () => {
    expect(true).toBe(true);
  });
});
